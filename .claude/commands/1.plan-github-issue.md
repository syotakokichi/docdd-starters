Please analyze and plan the implementation for {PROJECT} issue: {ISSUE}.

Follow these steps:

1. Inspect the latest context with `gh issue view {ISSUE} --comments`.
2. Summarize the problem, required outputs, and any acceptance criteria.
3. Locate the affected code or documents (7-axis docs, tests, CI, etc.).
4. Outline the implementation plan with clear tasks, owners, and checkpoints.
5. If the change involves an architectural decision, embed the ADR elements directly in the issue (Title / Context / Decision / Consideration / Consequences / References).
6. For new DocDD assets or updates, include a traceability work plan referencing:
   - `docs/7-axis/README.md` + `docs/7-axis/7_TC/TS-SAMPLE-001.md`
   - `docs/testing/traceability/<domain>_map.json`
   - Validation command: `python scripts/test/validate_traceability_map.py --map docs/testing/traceability/<domain>_map.json`
7. Document how the change will be verified:
   - Backend/frontend test commands (`.claude/commands/run-tests.md`)
   - Sheets sync flow (`.claude/commands/sync-test-sheets.md`)
   - Manual checks if required
8. Create or update an implementation plan issue titled `[{PROJECT}] {ISSUE} 実装計画` and link it back to {ISSUE}. For long plans, draft the body in `/tmp/{ISSUE}_plan.md` and feed it to `gh issue create --body-file`.
9. Apply appropriate labels (Priority + Content such as バックエンド/フロントエンド/テスト/ドキュメント/インフラ/バグ).

## Implementation Checklist (テンプレート)

```markdown
- [ ] DocDD: BR → UC → DM → SR/NSR → EXT → API → TS を列挙し、`docs/testing/traceability/<domain>_map.json` を更新
- [ ] 実装: バックエンド／フロントエンド／インフラなど担当範囲を明示し、関連コマンドを `.claude/commands/run-tests.md` にリンク
- [ ] テスト: `pytest` / `npm test` / `make traceability` など再現可能なコマンドを記載（TC ID / pytest_id を併記）
- [ ] Google Sheets: `.claude/commands/sync-test-sheets.md` に沿って `scripts/test/auto_sync_test_sheets.sh` の dry-run → 本番タスクを追加
- [ ] Validation: `python scripts/test/validate_traceability_map.py --map docs/testing/traceability/<domain>_map.json`
```

Use the GitHub CLI (`gh`) for every GitHub interaction to keep the workflow reproducible.

---
description: '[deprecated] /worktree に移行しました（ブランチ作成は worktree に統合済み）。'
argument-hint: "<issue-number>"
disable-model-invocation: true
---

# [deprecated] /3.create-branch

このコマンドは **`/worktree`** に置き換わりました（ブランチ作成は worktree フローに統合）。

> ⚠️ **このコマンドはここで処理を停止します。自動転送は行いません。**
> ユーザーが手動で `/worktree $ARGUMENTS` を再実行してください。

詳細: `.claude/commands/worktree.md`

> 削除予定: Phase E 完了 + 1 週間後（Issue F-1 で物理削除）
